import ClipLoader from "react-spinners/ClipLoader";

const LoadingClipLoader = () => {
    return (
        <div className="flex justify-center items-center h-full m-auto">
            <ClipLoader />
        </div>
    );
};

export default LoadingClipLoader;
